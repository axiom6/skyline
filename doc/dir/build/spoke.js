// Generated by CoffeeScript 1.10.0
var Spoke;

Spoke = (function() {
  function Spoke() {}

  Spoke.pdfCSS = function(href) {
    var link;
    if (!window.location.search.match(/pdf/gi)) {
      return;
    }
    link = document.createElement('link');
    link.rel = 'stylesheet';
    link.type = 'text/css';
    link.href = href;
    document.getElementsByTagName('head')[0].appendChild(link);
  };

  Spoke.center = function(selector) {
    var child, elem, elems, flag, horz, i, j, len, len1, ref, vert;
    elems = document.querySelectorAll(selector);
    for (i = 0, len = elems.length; i < len; i++) {
      elem = elems[i];
      flag = elem.getAttribute('data-center');
      if ((flag != null) && flag === 'none') {
        continue;
      }
      horz = document.createElement("div");
      vert = document.createElement("div");
      horz.className = 'bespoke-horz';
      vert.className = 'bespoke-vert';
      ref = elem.childNodes;
      for (j = 0, len1 = ref.length; j < len1; j++) {
        child = ref[j];
        vert.appendChild(child.cloneNode(true));
      }
      while (elem.hasChildNodes()) {
        elem.removeChild(elem.lastChild);
      }
      elem.appendChild(horz);
      horz.appendChild(vert);
    }
  };

  return Spoke;

})();
